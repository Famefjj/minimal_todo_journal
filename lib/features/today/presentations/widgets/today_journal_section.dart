import 'package:flutter/material.dart';

class TodayJournalSection extends StatelessWidget {
  const TodayJournalSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
